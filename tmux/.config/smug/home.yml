# ~/.config/smug/home.yml

session: home
root: ~/src
env:
  FOO: BAR
# tmux_options: -2

windows:
  - name: dot
    root: ~/src/github.com/seiji/dotfiles
  - name: pri
    root: ~/src/github.com/seiji/infra
  - name: vpri
    root: ~/src/github.com/seiji/infra
    commands:
      - vi .
